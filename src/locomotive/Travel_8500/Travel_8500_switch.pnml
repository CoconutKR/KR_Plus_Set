/*--------------------------------------------------------------------
스위치
--------------------------------------------------------------------*/
switch(FEAT_TRAINS, SELF, sw_Travel_8500_cargo_subtype_text, cargo_subtype) {
    1: return string(STR_REFIT_LIVERY_Travel_8500_G_train);       // G-Train
    2: return string(STR_REFIT_LIVERY_Travel_8500_S_train);       // S-Train
    3: return string(STR_REFIT_LIVERY_Travel_8500_V_train);       // V-Tain
    4: return string(STR_REFIT_LIVERY_Travel_8500_Haerang_train); // Haerang
    5: return string(STR_REFIT_LIVERY_Travel_8500_Wine_train);    // Wine
    return string(STR_REFIT_LIVERY_Travel_8500_A_train);
}

switch(FEAT_TRAINS, SELF, sw_Travel_8500_engine, cargo_subtype) {
    1: set_G_Train_engine;       // G-Train
    2: set_S_Train_engine;       // S-Train
    3: set_V_Train_engine;       // V-Tain
    4: set_Haerang_Train_engine; // Haerang
    5: set_Wine_Train_engine;    // Wine
    set_A_Train_engine;
}